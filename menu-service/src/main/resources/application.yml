server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/menu
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update

kafka:
  bootstrap-servers: localhost:9092
  topic: menu
